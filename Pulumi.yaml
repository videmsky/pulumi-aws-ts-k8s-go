name: hello-go
runtime: nodejs
description: hellow world go/gin app
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2
    hello_message:
      description: The message to return
      default: "Hello, Pulumi"
